#include<iostream>
#include<vector>
#include<algorithm> 
using namespace std;
int main() {
    // c=binary_search(a,a+9,40) f=upper_bound(a,a+9,10)
    // pos = lower_bound( number, number + 8, 3)
    cout << 1 << endl;
    return 0;
}
